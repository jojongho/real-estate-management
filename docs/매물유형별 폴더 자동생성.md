---
type: project
category:
  - 프로젝트
status: 진행중
priority:
start_date: 2025-11-02
due_date:
completion: 75%
tags:
  - 프로젝트
index:
  - "[[🏷 프로젝트]]"
tech_stack:
  - Apps Script
  - Google Sheets
  - JavaScript
  - Appsheet
---

## 통합 스크립트 구조
현재 `folder_create.js`는 매물 유형별로 통합된 단일 스크립트로 관리됩니다.
- `onEdit` 트리거에서 시트명으로 자동 분기 처리
- `onChange` 트리거로 AppSheet에서 데이터 추가 시에도 자동 처리
- 매물 유형별 최상위 폴더 ID는 `ROOT_FOLDER_IDS` 객체로 관리
- 공통 함수(`getOrCreateFolder`, `ensureResultColumnsForRelatedFile_` 등) 재사용
- ✅ **고정 컬럼 위치**: 모든 매물 시트에서 동일한 구조 사용 (AppSheet 스키마 동기화)
  - A열: ID (고정)
  - B열: 관련파일 (고정)  
  - C열: 폴더ID (고정)
- 이 구조는 AppSheet에서 "Regenerate Structure" 실행 후 스키마와 완벽히 일치합니다
## 아파트매물 v.1 완성버전
```js
const ROOT_FOLDER_ID = '1la_bddtVl7cpSCL8Leu-5ceeqXV5uuBO'; // 1-1. 아파트

// 시트 기반 실행 설정값
const SHEET_NAME = '아파트매물';             // 대상 시트명
const HEADER = {
  시군구: '시군구',
  동읍면: '동읍면',
  통반리: '통반리',
  지번: '지번',
  단지명: '단지명',
  동: '동',
  호: '호',
  타입: '타입',
  관련파일: '관련파일',       // 폴더 URL을 기록할 대상 열(예: W열)
  폴더ID: '폴더ID'           // 선택: 있으면 기록, 없으면 건너뜀
};

// 고정 위치(존재하지 않으면 해당 위치에 헤더를 생성)
const RELATED_FILE_COLUMN_LETTER = 'W';   // 관련파일은 W열
const FOLDER_ID_COLUMN_LETTER = 'X';     // 폴더ID는 X열(통일)

function columnLetterToIndex_(letter) {
  var col = 0;
  for (var i = 0; i < letter.length; i++) {
    col = col * 26 + (letter.charCodeAt(i) - 64);
  }
  return col; // 1-based index
}

/**
 * AppSheet Webhook endpoint
 */
function doPost(e) {
  try {
    // POST 데이터 파싱
    const data = JSON.parse(e.postData.contents);

    // 필수 파라미터 검증 (통반리는 선택적)
    if (!data.시군구 || !data.동읍면 || !data.지번 || !data.단지명 || !data.동 || !data.호 || !data.타입) {
      return ContentService.createTextOutput(JSON.stringify({
        'status': 'error',
        'message': '필수 파라미터가 누락되었습니다.'
      })).setMimeType(ContentService.MimeType.JSON);
    }

    // 폴더 생성 (통반리는 선택적 파라미터)
    const folderUrl = createPropertyFolderStructure(
      data.시군구,
      data.동읍면,
      data.통반리 || '',  // 통반리가 없으면 빈 문자열
      data.지번,
      data.단지명,
      data.동,
      data.호,
      data.타입
    );

    // 성공 응답
    return ContentService.createTextOutput(JSON.stringify({
      'status': 'success',
      'folderUrl': folderUrl.url,
      'folderId': folderUrl.id
    })).setMimeType(ContentService.MimeType.JSON);

  } catch (error) {
    Logger.log('Error: ' + error.toString());
    return ContentService.createTextOutput(JSON.stringify({
      'status': 'error',
      'message': error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

/**
 * 설치형 onEdit 트리거 진입점
 * - 시트에서 행이 생성/수정될 때 폴더 자동 생성 및 URL/ID 기록
 */
function onEdit(e) {
  try {
    handleSheetEdit_(e);
  } catch (error) {
    Logger.log('onEdit error: ' + error);
  }
}

/**
 * 편집된 행을 검사해 필요한 값이 모두 채워졌고 폴더 정보가 비어 있으면 생성
 */
function handleSheetEdit_(e) {
  const sheet = e.range.getSheet();
  if (sheet.getName() !== SHEET_NAME) return;
  const editedRow = e.range.getRow();
  if (editedRow === 1) return; // 헤더 행 제외

  const header = sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
  const colIndex = (name) => header.indexOf(name) + 1; // 1-based

  // 필수값: 시군구, 동읍면, 지번, 단지명, 동, 호, 타입 모두 필요
  const requiredHeaders = [HEADER.시군구, HEADER.동읍면, HEADER.지번, HEADER.단지명, HEADER.동, HEADER.호, HEADER.타입];
  for (var i = 0; i < requiredHeaders.length; i++) {
    if (colIndex(requiredHeaders[i]) < 1) return; // 필수 헤더 없으면 중단
  }

  // 결과 컬럼 찾기(관련파일은 반드시 존재해야 함. 없으면 생성)
  const ensured = ensureResultColumnsForRelatedFile_(sheet, header);
  const urlCol = ensured.urlCol; // 관련파일 열
  const idCol = ensured.idCol;   // 폴더ID가 없을 수 있음(없으면 0)

  // 현재 행 값 읽기
  const rowValues = sheet.getRange(editedRow, 1, 1, sheet.getLastColumn()).getValues()[0];
  const getVal = (name) => rowValues[colIndex(name) - 1];

  // 필수값 체크
  const values = {
    시군구: getVal(HEADER.시군구),
    동읍면: getVal(HEADER.동읍면),
    통반리: colIndex(HEADER.통반리) > 0 ? getVal(HEADER.통반리) : '',
    지번:   getVal(HEADER.지번),
    단지명: getVal(HEADER.단지명),
    동: getVal(HEADER.동),
    호: getVal(HEADER.호),
    타입: getVal(HEADER.타입)
  };

  var hasAllRequired = values.시군구 && values.동읍면 && values.지번 && values.단지명 && values.동 && values.호 && values.타입;
  if (!hasAllRequired) return;

  // 이미 폴더 정보가 있으면 건너뜀
  const currentUrl = rowValues[urlCol - 1];
  const currentId = idCol > 0 ? rowValues[idCol - 1] : '';
  if (currentUrl) return; // URL이 이미 있으면 생성 안 함

  // 폴더 생성
  const folder = createPropertyFolderStructure(
    values.시군구,
    values.동읍면,
    values.통반리 || '',
    values.지번,
    values.단지명,
    values.동,
    values.호,
    values.타입
  );

  // 결과 기록
  sheet.getRange(editedRow, urlCol).setValue(folder.url);
  if (idCol > 0) sheet.getRange(editedRow, idCol).setValue(folder.id);
}

/**
 * 폴더 구조 생성 메인 함수
 */
function createPropertyFolderStructure(시군구, 동읍면, 통반리, 지번, 단지명, 동, 호, 타입) {
  const rootFolder = DriveApp.getFolderById(ROOT_FOLDER_ID);

  // 1단계: 시군구 폴더
  const 시군구Folder = getOrCreateFolder(rootFolder, 시군구);

  // 2단계: 동읍면 폴더
  const 동읍면Folder = getOrCreateFolder(시군구Folder, 동읍면);

  // 3단계: 통반리 폴더 (통반리 정보가 있을 때만 생성)
  let 부모Folder = 동읍면Folder;

  if (통반리 && 통반리.trim() !== '') {
    부모Folder = getOrCreateFolder(동읍면Folder, 통반리);
  }

  // 4단계: 지번 단지명 폴더
  const 단지Folder = getOrCreateFolder(부모Folder, `${지번} ${단지명}`);

  // 5단계: 매물 폴더 (고정, 하이픈으로 시작하여 정렬 시 맨 앞에 위치)
  const 매물Folder = getOrCreateFolder(단지Folder, '-매물');

  // 6단계: 동-호-타입 폴더
  const 최종Folder = getOrCreateFolder(매물Folder, `${동}-${호}-${타입}`);

  return {
    url: 최종Folder.getUrl(),
    id: 최종Folder.getId()
  };
}

/**
 * 폴더 가져오기 또는 생성
 * @param {Folder} parentFolder - 부모 폴더
 * @param {string} folderName - 생성할 폴더 이름
 * @return {Folder} - 폴더 객체
 */
function getOrCreateFolder(parentFolder, folderName) {
  // 기존 폴더 검색
  const folders = parentFolder.getFoldersByName(folderName);

  if (folders.hasNext()) {
    // 이미 존재하면 기존 폴더 반환
    return folders.next();
  } else {
    // 없으면 새로 생성
    return parentFolder.createFolder(folderName);
  }
}

/**
 * 테스트 함수 (Apps Script 에디터에서 직접 실행)
 */
function testCreateFolder() {
  // 테스트 1: 통반리가 있는 경우
  Logger.log('=== 테스트 1: 통반리 있음 ===');
  const result1 = createPropertyFolderStructure(
    '천안시 서북구',     // 시군구
    '두정동',           // 동읍면
    '두정동',           // 통반리
    '37-1',             // 지번
    '힐스테이트두정역', // 단지명
    '101',              // 동
    '1001',             // 호
    '84A'               // 타입
  );
  Logger.log('폴더 생성 완료!');
  Logger.log('URL: ' + result1.url);
  Logger.log('ID: ' + result1.id);

  // 테스트 2: 통반리가 없는 경우 (건너뜀)
  Logger.log('\n=== 테스트 2: 통반리 없음 (건너뜀) ===');
  const result2 = createPropertyFolderStructure(
    '천안시 동남구',  // 시군구
    '신부동',         // 동읍면
    '',               // 통반리 (빈 문자열 = 폴더 건너뜀)
    '123',            // 지번
    '테스트단지',     // 단지명
    '201',            // 동
    '2001',           // 호
    '105A'            // 타입
  );
  Logger.log('폴더 생성 완료!');
  Logger.log('URL: ' + result2.url);
  Logger.log('ID: ' + result2.id);
}

/**
 * 시트 전체를 스캔하여 폴더URL/ID가 비어 있는 행에 대해 일괄 생성
 */
function backfillPropertyFolders() {
  try {
    // 활성 스프레드시트 가져오기
    const ss = SpreadsheetApp.getActiveSpreadsheet();
    if (!ss) {
      Logger.log('오류: 활성 스프레드시트를 찾을 수 없습니다.');
      return;
    }
    
    const sheet = ss.getSheetByName(SHEET_NAME);
    if (!sheet) {
      Logger.log('오류: 시트 "' + SHEET_NAME + '"를 찾을 수 없습니다.');
      return;
    }
    
    Logger.log('시트 발견: ' + SHEET_NAME);
    
    const lastRow = sheet.getLastRow();
    const lastCol = sheet.getLastColumn();
    if (lastRow < 2) {
      Logger.log('경고: 데이터 행이 없습니다. (lastRow: ' + lastRow + ')');
      return;
    }
    
    Logger.log('데이터 범위: 2행부터 ' + lastRow + '행, ' + lastCol + '열');

    const data = sheet.getRange(1, 1, lastRow, lastCol).getValues();
    const header = data[0];
    const colIndex = (name) => header.indexOf(name) + 1;

    // 헤더 확인 및 로깅
    const req = [HEADER.시군구, HEADER.동읍면, HEADER.지번, HEADER.단지명, HEADER.동, HEADER.호, HEADER.타입];
    var missingHeaders = [];
    for (var i = 0; i < req.length; i++) {
      if (colIndex(req[i]) < 1) {
        missingHeaders.push(req[i]);
      }
    }
    
    if (missingHeaders.length > 0) {
      Logger.log('오류: 필수 헤더가 없습니다: ' + missingHeaders.join(', '));
      Logger.log('현재 헤더: ' + header.join(', '));
      return;
    }

    Logger.log('필수 헤더 확인 완료');

    const ensured = ensureResultColumnsForRelatedFile_(sheet, header);
    const urlCol = ensured.urlCol;
    const idCol = ensured.idCol;
    
    Logger.log('결과 열 위치 - 관련파일: ' + urlCol + '열, 폴더ID: ' + idCol + '열');

    const updatesUrl = [];
    const updatesId = [];
    const targetRows = [];
    var processedCount = 0;
    var skippedCount = 0;
    var skippedByUrl = 0;
    var skippedByMissingData = 0;
    var firstSkippedRow = null;

    for (var r = 2; r <= lastRow; r++) {
      const row = data[r - 1];
      
      // 이미 URL이 있으면 건너뜀
      if (row[urlCol - 1]) {
        skippedByUrl++;
        skippedCount++;
        if (!firstSkippedRow) {
          firstSkippedRow = {
            row: r,
            reason: 'URL이 이미 존재',
            url: row[urlCol - 1]
          };
        }
        continue;
      }
      
      // 필수값 체크
      const 시군구 = row[colIndex(HEADER.시군구) - 1];
      const 동읍면 = row[colIndex(HEADER.동읍면) - 1];
      const 지번 = row[colIndex(HEADER.지번) - 1];
      const 단지명 = row[colIndex(HEADER.단지명) - 1];
      const 동 = row[colIndex(HEADER.동) - 1];
      const 호 = row[colIndex(HEADER.호) - 1];
      const 타입 = row[colIndex(HEADER.타입) - 1];
      
      var ok = 시군구 && 동읍면 && 지번 && 단지명 && 동 && 호 && 타입;
      
      if (!ok) {
        skippedByMissingData++;
        skippedCount++;
        if (!firstSkippedRow) {
          var missingFields = [];
          if (!시군구) missingFields.push('시군구');
          if (!동읍면) missingFields.push('동읍면');
          if (!지번) missingFields.push('지번');
          if (!단지명) missingFields.push('단지명');
          if (!동) missingFields.push('동');
          if (!호) missingFields.push('호');
          if (!타입) missingFields.push('타입');
          firstSkippedRow = {
            row: r,
            reason: '필수값 누락',
            missing: missingFields.join(', ')
          };
        }
        continue;
      }

      // 폴더 생성
      const folder = createPropertyFolderStructure(
        시군구,
        동읍면,
        colIndex(HEADER.통반리) > 0 ? row[colIndex(HEADER.통반리) - 1] : '',
        지번,
        단지명,
        동,
        호,
        타입
      );
      
      updatesUrl.push([folder.url]);
      updatesId.push([folder.id]);
      targetRows.push(r);
      processedCount++;
    }
    
    Logger.log('=== 처리 결과 ===');
    Logger.log('처리된 행: ' + processedCount + '개');
    Logger.log('건너뛴 행: ' + skippedCount + '개');
    Logger.log('  - URL 이미 존재: ' + skippedByUrl + '개');
    Logger.log('  - 필수값 누락: ' + skippedByMissingData + '개');
    
    if (firstSkippedRow) {
      Logger.log('첫 번째 건너뛴 행 (예시): ' + firstSkippedRow.row + '행 - ' + firstSkippedRow.reason);
      if (firstSkippedRow.missing) {
        Logger.log('  누락된 필드: ' + firstSkippedRow.missing);
      }
    }
    
    // 배치 기록
    for (var i = 0; i < targetRows.length; i++) {
      sheet.getRange(targetRows[i], urlCol).setValue(updatesUrl[i][0]);
      if (idCol > 0) sheet.getRange(targetRows[i], idCol).setValue(updatesId[i][0]);
    }
    
    Logger.log('완료: ' + targetRows.length + '개 행에 폴더 정보를 기록했습니다.');
    
  } catch (error) {
    Logger.log('오류 발생: ' + error.toString());
    Logger.log(error.stack);
  }
}


/**
 * 관련파일 열을 보장하고, 폴더ID는 있으면 인덱스를 반환
 * W열(관련파일), X열(폴더ID)에 고정 생성
 */
function ensureResultColumnsForRelatedFile_(sheet, headerRow) {
  var header = headerRow || sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
  var urlCol = header.indexOf(HEADER.관련파일) + 1;
  var idCol = header.indexOf(HEADER.폴더ID) + 1;

  // 관련파일: 없으면 W열에 생성
  if (urlCol < 1) {
    var relatedTarget = columnLetterToIndex_(RELATED_FILE_COLUMN_LETTER);
    sheet.getRange(1, relatedTarget).setValue(HEADER.관련파일);
    urlCol = relatedTarget;
  }

  // 폴더ID: 없으면 X열에 생성
  if (idCol < 1) {
    var idTarget = columnLetterToIndex_(FOLDER_ID_COLUMN_LETTER);
    sheet.getRange(1, idTarget).setValue(HEADER.폴더ID);
    idCol = idTarget;
  }

  return { urlCol: urlCol, idCol: idCol };
}

/**
 * 매물 삭제 시 폴더도 삭제 (선택사항)
 */
function doDelete(e) {
  try {
    const data = JSON.parse(e.postData.contents);

    if (!data.folderId) {
      return ContentService.createTextOutput(JSON.stringify({
        'status': 'error',
        'message': 'folderId가 필요합니다.'
      })).setMimeType(ContentService.MimeType.JSON);
    }

    const folder = DriveApp.getFolderById(data.folderId);
    folder.setTrashed(true);

    return ContentService.createTextOutput(JSON.stringify({
      'status': 'success',
      'message': '폴더가 삭제되었습니다.'
    })).setMimeType(ContentService.MimeType.JSON);

  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({
      'status': 'error',
      'message': error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

```

## 주택타운 v.1 완성버전

### 구현 완료 상태
✅ 통합 스크립트에 주택타운 폴더 생성 기능이 구현되어 동작 중입니다.

### 최상위 폴더
- 폴더명: `1-2. 주택타운하우스`
- 폴더 ID: `1xmd2CB1umokrkAUnz-kJshtewLDFg-is`

### 공통 생성 규칙
- 최상위 폴더부터 행정구역 폴더 구조는 아파트매물과 동일
  - `[1-2. 주택타운하우스]/[시군구]/[동읍면]/[통반리(선택)]`
- `주택유형` 칼럼 값을 참고하여 주택유형별로 다른 폴더 생성 규칙 적용
- **주택단지 폴더명**: 지번 제거, 단지명만 사용 (예: `캐슬카운티1차`, `캠스베일리1차`)
- **리프 폴더명**: 단위 포함 형식 사용 (`번지`, `동`, `호` 명시)

### 단독주택 생성 규칙
- 최종 폴더는 `-매물` 폴더 없이 생성
- 폴더명: `[지번번지 단독]` (공백으로 구분)
- 주택유형이 '단독'이거나 주택단지 값이 '단독'인 경우 적용

> [!example]
> `[1-2. 주택타운하우스]/[아산시]/[도고면]/[시전리]/[1487번지 단독]`

### 단지형 전원주택 생성 규칙
기본적으로 단독주택과 마찬가지로 1개의 필지가 단독으로 건축되는 형태이나, 개발업자가 통일되게 조성한 단지 형태입니다.

- 주택단지 폴더: `[단지명]` (지번 제외)
- 매물 폴더: `-매물`
- 리프 폴더: `[지번번지 동동 호호]` (동/호 없으면 지번번지만)
  - 지번: `지번번지` 형식 (예: `253-41번지`)
  - 동: `동동` 형식 (예: `106동`)
  - 호: `호호` 형식 (예: `103호`)
  - 공백으로 구분

> [!example]
> `[1-2. 주택타운하우스]/[천안시 서북구]/[성거읍]/[소우리]/[크레아타운]/[-매물]/[253-41번지 106동 103호]`

### 빌라, 타운하우스, 듀플렉스 주택 생성 규칙
하나의 필지에 수개의 동이 있는 형태입니다. 도로명 주소는 기록하지 않고 폴더 구조로만 관리합니다.

- 주택단지 폴더: `[단지명]` (지번 제외)
- 매물 폴더: `-매물`
- 리프 폴더: `[지번번지 동동 호호 타입]` (없는 값은 제외)
  - 지번: `지번번지` 형식 (예: `1352-7번지`)
  - 동: `동동` 형식 (예: `106동`)
  - 호: `호호` 형식 (예: `3호`)
  - 타입: 타입 값 그대로 (단위 없음)
  - 공백으로 구분

> [!example]
> `[1-2. 주택타운하우스]/[아산시]/[배방읍]/[장재리]/[캠스베일리1차]/[-매물]/[1352-7번지 106동 3호]`

### 필수 시트 칼럼
- 시군구, 동읍면, 지번, 주택단지 (필수)
- 주택유형, 통반리 (선택)
- 동, 호, 타입 (주택유형에 따라 선택)
- 관련파일 (W열), 폴더ID (X열) - 자동 생성

### 사용 방법
1. **자동 생성**: 주택타운 시트에서 필수값 입력 시 `onEdit` 트리거로 자동 폴더 생성
2. **AppSheet 연동**: AppSheet에서 데이터 추가 시 `onChange` 트리거로 자동 처리
3. **일괄 보정**: 주택타운 시트를 활성화한 상태에서 `backfillPropertyFolders()` 함수 실행

## 건물/상가/원투룸 v.1 완성버전

### 구현 완료 상태
✅ 통합 스크립트에 건물/상가/원투룸 폴더 생성 기능이 구현되어 동작 중입니다.

### 최상위 폴더
- 폴더명: `1-3. 근생(건물, 상가, 원투룸)`
- 폴더 ID: `15C9WC3Og9kcwpntfDwBgT9jVr1aDOIfD`

### 공통 생성 규칙
- 최상위 폴더부터 행정구역 폴더 구조는 아파트매물과 동일
  - `[1-3. 근생]/[시군구]/[동읍면]/[통반리(선택)]/[지번 건물명]`
- 건물 시트는 행정구역 칼럼이 분리되어 있음 (시군구, 동읍면, 통반리, 지번)
- 상가/원투룸 시트는 행정구역 칼럼이 없어서 건물명으로 건물 시트를 조회하여 주소 정보 획득
- 상가/원투룸 시트의 건물명이 건물 시트에 없을 경우 주소 칼럼을 파싱하여 주소 정보 추출

### 폴더 구조
- **건물 폴더**: `[지번 건물명]`
- **매물 폴더**: `-매물` (통합)
- **리프 폴더**:
  - 상가: `[호 상호명 거래유형]`
  - 원투룸: `[호 방구조 거래유형]`

> [!example]
> 상가: `[1-3. 근생]/[천안시 서북구]/[성정동]/[847 BN건설건물]/[-매물]/[101 사과가게 매매]`
> 원투룸: `[1-3. 근생]/[천안시 서북구]/[성정동]/[847 BN건설건물]/[-매물]/[201 원룸 전세]`

### 필수 시트 칼럼
- **건물 시트**: 시군구, 동읍면, 지번, 건물명 (필수), 통반리 (선택)
- **상가 시트**: 건물명, 호수, 상호명, 거래유형 (필수), 주소 (보조)
- **원투룸 시트**: 건물명, 호, 방구조, 거래유형 (필수), 주소 (보조)
- 관련파일/폴더ID 컬럼 위치:
  - 건물: 관련파일(AA열), 폴더ID(AS열)
  - 상가: 관련파일(Y열), 폴더ID(AE열)
  - 원투룸: 관련파일(U열), 폴더ID(X열)

### 사용 방법
1. **자동 생성**: 각 시트에서 필수값 입력 시 `onEdit` 트리거로 자동 폴더 생성
2. **AppSheet 연동**: AppSheet에서 데이터 추가 시 `onChange` 트리거로 자동 처리
3. **일괄 보정**: 해당 시트를 활성화한 상태에서 `backfillPropertyFolders()` 함수 실행

## 토지 v.1 완성버전

### 구현 완료 상태
✅ 통합 스크립트에 토지 폴더 생성 기능이 구현되어 동작 중입니다.

### 최상위 폴더
- 폴더명: `[토지 최상위 폴더]`
- 폴더 ID: `1yCdKd6UUnIwuWXhsfis_9HsL5uJfj3Ph`

### 폴더 구조
토지는 비교적 간단한 구조로 행정구역 폴더 아래에 바로 매물 폴더를 생성합니다.

- `[최상위]/[시군구]/[동읍면]/[통반리(선택)]/[지번 토지분류]`

> [!example]
> `[토지최상위폴더]/[아산시]/[탕정면]/[용두리]/[475-24 공장창고부지]`

### 필수 시트 칼럼
- 시군구, 동읍면, 지번, 토지분류 (필수)
- 통반리 (선택)
- 관련파일 (U열), 폴더ID (V열) - 자동 생성

### 사용 방법
1. **자동 생성**: 토지 시트에서 필수값 입력 시 `onEdit` 트리거로 자동 폴더 생성
2. **AppSheet 연동**: AppSheet에서 데이터 추가 시 `onChange` 트리거로 자동 처리
3. **일괄 보정**: 토지 시트를 활성화한 상태에서 `backfillPropertyFolders()` 함수 실행

## 공장창고 v.1 완성버전

### 구현 완료 상태
✅ 통합 스크립트에 공장창고 폴더 생성 기능이 구현되어 동작 중입니다.

### 최상위 폴더
- 폴더명: `[공장창고 최상위 폴더]`
- 폴더 ID: `1kWcFAasdHdhZDQ82XCC_slf3CZxuHpgU`

### 폴더 구조
공장창고는 토지와 동일하게 간단한 구조로 행정구역 폴더 아래에 바로 매물 폴더를 생성합니다.

- `[최상위]/[시군구]/[동읍면]/[통반리(선택)]/[지번 명칭]`

> [!example]
> `[공장창고최상위폴더]/[아산시]/[탕정면]/[갈산리]/[558-4 혜성밴딩 공장]`

### 필수 시트 칼럼
- 시군구, 동읍면, 지번, 명칭 (필수)
- 통반리 (선택)
- 관련파일 (AD열), 폴더ID (AE열) - 자동 생성

### 사용 방법
1. **자동 생성**: 공장창고 시트에서 필수값 입력 시 `onEdit` 트리거로 자동 폴더 생성
2. **AppSheet 연동**: AppSheet에서 데이터 추가 시 `onChange` 트리거로 자동 처리
3. **일괄 보정**: 공장창고 시트를 활성화한 상태에서 `backfillPropertyFolders()` 함수 실행

## 전체 구현 현황

### 완료된 매물 유형
1. ✅ **아파트매물**: 완전 구현, 운영 중
2. ✅ **주택타운**: 완전 구현, 운영 중
3. ✅ **건물/상가/원투룸**: 완전 구현, 운영 중
4. ✅ **토지**: 완전 구현, 운영 중
5. ✅ **공장창고**: 완전 구현, 운영 중

### 주요 기능
- ✅ `onEdit` 트리거: 시트 편집 시 자동 폴더 생성
- ✅ `onChange` 트리거: AppSheet 데이터 추가 시 자동 처리
- ✅ `backfillPropertyFolders()`: 일괄 보정 함수 (시트별 자동 분기)
- ✅ 시트별 관련파일/폴더ID 컬럼 위치 지정 (AppSheet 스키마 충돌 방지)
- ✅ 시군구 정규화 함수로 중복 폴더 방지
- ✅ 주소 파싱 기능 (건물명 기반 조회 실패 시 사용)

### 향후 개선 사항
- [ ] 각 매물 유형별 폴더 생성 테스트 완료
- [ ] 에러 처리 및 로깅 개선
- [ ] 폴더 삭제 기능 (매물 삭제 시)


