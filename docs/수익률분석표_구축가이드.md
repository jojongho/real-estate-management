# 수익률 분석표 구축 가이드

**작성일:** 2025-10-30
**버전:** 1.0
**목적:** 건물 및 상가 투자 수익률 분석 자동화

---

## 📋 목차

1. [개요](#개요)
2. [건물 테이블 설정](#건물-테이블-설정)
3. [상가 테이블 설정](#상가-테이블-설정)
4. [건물 수익률 분석표 생성](#건물-수익률-분석표-생성)
5. [상가 수익률 분석표 생성](#상가-수익률-분석표-생성)
6. [PDF 생성 및 공유](#pdf-생성-및-공유)
7. [고급: Apps Script 자동화](#고급-apps-script-자동화)

---

## 개요

### 목표
- **건물 통매매** 투자자를 위한 수익률 분석표
- **구분상가** 투자자를 위한 개별 상가 수익률 분석표
- Google Sheets 기반 템플릿으로 즉시 사용 가능

### 구조
```
스프레드시트
├── 건물 (원본 데이터 + 매물 칼럼)
├── 상가매물 (원본 데이터 + 수익률 칼럼)
├── 원투룸
├── 수익률분석표_건물 (템플릿)
└── 수익률분석표_상가 (템플릿)
```

---

## 건물 테이블 설정

### Step 1: Google Sheets에 칼럼 추가

**건물 시트에 다음 칼럼 추가:**

| 순서 | 칼럼명 | 초기값 | 비고 |
|------|--------|--------|------|
| 1 | 매물여부 | "매물아님" | |
| 2 | 거래유형 | | |
| 3 | 거래상태 | | |
| 4 | 매매가 | | |
| 5 | 임대료 | | |
| 6 | 보증금 | | |
| 7 | 등록일 | | |
| 8 | 계약일 | | |
| 9 | 메모 | | |

**방법:**
1. 건물 시트 열기
2. 기존 마지막 칼럼 다음에 위 칼럼들 추가
3. 헤더 행에 칼럼명 입력

---

### Step 2: AppSheet에서 칼럼 설정

#### 2-1. 데이터 동기화

**Data > Tables > 건물 > Regenerate Structure**

---

#### 2-2. 칼럼 타입 설정

**매물여부:**
```yaml
Type: Enum
Values: 매물아님, 매물
Initial value: 매물아님
Show: Yes
```

**거래유형:**
```yaml
Type: Enum
Values: 매매, 임대
Show_If: [매물여부] = "매물"
```

**거래상태:**
```yaml
Type: Enum
Values: 판매중, 계약진행중, 계약완료, 보류, 급매
Show_If: [매물여부] = "매물"
```

**매매가, 임대료, 보증금:**
```yaml
Type: Price
Show_If: [매물여부] = "매물"
```

**등록일, 계약일:**
```yaml
Type: Date
Show_If: [매물여부] = "매물"
```

**메모:**
```yaml
Type: LongText
Show_If: [매물여부] = "매물"
```

---

#### 2-3. 가상 칼럼 추가 (AppSheet에서만)

**총임대료_월:**
```yaml
Column name: 총임대료_월
Type: Number
Formula: SUM([Related 상가s][월세], [Related 원투룸s][월세])
Show: Yes
Show_If: [매물여부] = "매물"
Editable: No
```

**총보증금:**
```yaml
Column name: 총보증금
Type: Number
Formula: SUM([Related 상가s][보증금], [Related 원투룸s][보증금])
Show: Yes
Show_If: [매물여부] = "매물"
Editable: No
```

**연임대수익:**
```yaml
Column name: 연임대수익
Type: Number
Formula: [총임대료_월] * 12
Show: Yes
Show_If: [매물여부] = "매물"
Editable: No
```

**연수익률(%):**
```yaml
Column name: 연수익률(%)
Type: Decimal
Formula: IF([매매가] > 0, ([연임대수익] / [매매가]) * 100, 0)
Show: Yes
Show_If: AND([매물여부] = "매물", [거래유형] = "매매")
Editable: No
```

**투자회수기간(년):**
```yaml
Column name: 투자회수기간(년)
Type: Decimal
Formula: IF([연임대수익] > 0, [매매가] / [연임대수익], 0)
Show: Yes
Show_If: AND([매물여부] = "매물", [거래유형] = "매매")
Editable: No
```

**총호실수:**
```yaml
Column name: 총호실수
Type: Number
Formula: COUNT([Related 상가s]) + COUNT([Related 원투룸s])
Show: Yes
Show_If: [매물여부] = "매물"
Editable: No
```

**임대중호실:**
```yaml
Column name: 임대중호실
Type: Number
Formula: COUNT(SELECT([Related 상가s][_RowNumber], [거래상태]="임대중")) + COUNT(SELECT([Related 원투룸s][_RowNumber], [거래상태]="임대중"))
Show: Yes
Show_If: [매물여부] = "매물"
Editable: No
```

**공실수:**
```yaml
Column name: 공실수
Type: Number
Formula: [총호실수] - [임대중호실]
Show: Yes
Show_If: [매물여부] = "매물"
Editable: No
```

**임대율(%):**
```yaml
Column name: 임대율(%)
Type: Decimal
Formula: IF([총호실수] > 0, ([임대중호실] / [총호실수]) * 100, 0)
Show: Yes
Show_If: [매물여부] = "매물"
Editable: No
```

---

#### 2-4. DP거래 수식 (건물용)

```yaml
Column name: DP거래
Type: Text
Formula:
IF(
  [매물여부] = "매물",
  TRIM(
    CONCATENATE(
      IF([거래유형] = "매매", CONCATENATE("매매 ", TEXT([매매가]), " "), ""),
      IF([거래유형] = "임대", CONCATENATE("보 ", TEXT([보증금]), " 월 ", TEXT([임대료])), "")
    )
  ),
  ""
)
Show: Yes
Editable: No
```

---

## 상가 테이블 설정

### Step 1: AppSheet 가상 칼럼 추가

**연임대수익:**
```yaml
Column name: 연임대수익
Type: Number
Formula: [월세] * 12
Show: Yes
Editable: No
```

**연수익률(%):**
```yaml
Column name: 연수익률(%)
Type: Decimal
Formula: IF([매매가] > 0, ([연임대수익] / [매매가]) * 100, 0)
Show: Yes
Show_If: ISNOTBLANK([매매가])
Editable: No
```

**투자회수기간(년):**
```yaml
Column name: 투자회수기간(년)
Type: Decimal
Formula: IF([연임대수익] > 0, [매매가] / [연임대수익], 0)
Show: Yes
Show_If: ISNOTBLANK([매매가])
Editable: No
```

**평당가격:**
```yaml
Column name: 평당가격
Type: Price
Formula: IF([면적(평)] > 0, [매매가] / [면적(평)], 0)
Show: Yes
Show_If: ISNOTBLANK([매매가])
Editable: No
```

**평당월세:**
```yaml
Column name: 평당월세
Type: Price
Formula: IF([면적(평)] > 0, [월세] / [면적(평)], 0)
Show: Yes
Editable: No
```

---

## 건물 수익률 분석표 생성

### Step 1: 새 시트 생성

**Google Sheets에서:**
1. 스프레드시트 하단 "+" 클릭
2. 시트 이름: `수익률분석표_건물`

---

### Step 2: 템플릿 레이아웃 구성

#### A. 헤더 섹션 (A1:G5)

```
┌─────────────────────────────────────────────────┐
│              건물 투자 수익률 분석표             │
│                                                 │
│  작성일: [자동]          담당자: [입력]         │
│                                                 │
│  건물 선택: [드롭다운] ▼                        │
└─────────────────────────────────────────────────┘
```

**셀 설정:**
- A1: "건물 투자 수익률 분석표" (병합: A1:G1, 가운데 정렬, 크기 18pt, 굵게)
- A3: "작성일:"
- B3: `=TODAY()` (날짜 포맷)
- D3: "담당자:"
- E3: (직접 입력)
- A5: "건물 선택:"
- B5: 드롭다운 (Data > Data validation > List from range: `건물!B2:B100`)

---

#### B. 기본정보 섹션 (A7:G12)

```
┌─────────────────────────────────────────────────┐
│ 1. 건물 기본 정보                               │
├─────────────────────────────────────────────────┤
│  건물명:      [참조]                            │
│  주소:        [참조]                            │
│  건축년도:    [참조]          총층수: [참조]    │
│  용도:        [참조]                            │
└─────────────────────────────────────────────────┘
```

**수식:**

**A7:** "1. 건물 기본 정보" (병합: A7:G7, 배경색: 연한회색, 굵게)

**A9:** "건물명:"
**B9:** `=B5`

**A10:** "주소:"
**B10:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("주소", 건물!$1:$1, 0), FALSE), "")`

**A11:** "건축년도:"
**B11:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("건축년도", 건물!$1:$1, 0), FALSE), "")`

**D11:** "총층수:"
**E11:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("총층수", 건물!$1:$1, 0), FALSE), "")`

**A12:** "용도:"
**B12:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("건축물용도", 건물!$1:$1, 0), FALSE), "")`

---

#### C. 매물정보 섹션 (A14:G18)

```
┌─────────────────────────────────────────────────┐
│ 2. 매물 정보                                    │
├─────────────────────────────────────────────────┤
│  거래유형:    [참조]          거래상태: [참조]  │
│  매매가:      [참조]                   5억원    │
│  등록일:      [참조]                            │
└─────────────────────────────────────────────────┘
```

**수식:**

**A14:** "2. 매물 정보" (병합, 배경색, 굵게)

**A16:** "거래유형:"
**B16:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("거래유형", 건물!$1:$1, 0), FALSE), "")`

**D16:** "거래상태:"
**E16:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("거래상태", 건물!$1:$1, 0), FALSE), "")`

**A17:** "매매가:"
**B17:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("매매가", 건물!$1:$1, 0), FALSE), 0)`
(포맷: 통화, #,##0"만원")

**A18:** "등록일:"
**B18:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("등록일", 건물!$1:$1, 0), FALSE), "")`

---

#### D. 수익률 분석 섹션 (A20:G30)

```
┌─────────────────────────────────────────────────┐
│ 3. 수익률 분석                                  │
├─────────────────────────────────────────────────┤
│  총 호실수:        15개                         │
│  임대중:           13개                         │
│  공실:             2개                          │
│  임대율:           86.7%                        │
│                                                 │
│  월 임대수익:      300만원                      │
│  연 임대수익:      3,600만원                    │
│                                                 │
│  연 수익률:        7.2%        ⭐              │
│  투자회수기간:     13.9년                       │
└─────────────────────────────────────────────────┘
```

**수식:**

**A20:** "3. 수익률 분석" (병합, 배경색, 굵게)

**A22:** "총 호실수:"
**B22:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("총호실수", 건물!$1:$1, 0), FALSE), 0) & "개"`

**A23:** "임대중:"
**B23:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("임대중호실", 건물!$1:$1, 0), FALSE), 0) & "개"`

**A24:** "공실:"
**B24:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("공실수", 건물!$1:$1, 0), FALSE), 0) & "개"`

**A25:** "임대율:"
**B25:** `=TEXT(IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("임대율(%)", 건물!$1:$1, 0), FALSE), 0)/100, "0.0%")`

**A27:** "월 임대수익:"
**B27:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("총임대료_월", 건물!$1:$1, 0), FALSE), 0)`
(포맷: #,##0"만원")

**A28:** "연 임대수익:"
**B28:** `=IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("연임대수익", 건물!$1:$1, 0), FALSE), 0)`
(포맷: #,##0"만원")

**A30:** "연 수익률:"
**B30:** `=TEXT(IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("연수익률(%)", 건물!$1:$1, 0), FALSE), 0)/100, "0.00%")`
(배경색: 노란색, 굵게)

**A31:** "투자회수기간:"
**B31:** `=TEXT(IFERROR(VLOOKUP($B$5, 건물!$B:$Z, MATCH("투자회수기간(년)", 건물!$1:$1, 0), FALSE), 0), "0.0") & "년"`

---

#### E. 호실별 현황 섹션 (A33:G50)

```
┌─────────────────────────────────────────────────┐
│ 4. 호실별 임대 현황                             │
├──────┬──────┬──────┬──────┬──────┬──────┤
│ 호실 │ 용도 │ 면적 │ 보증금│ 월세 │ 상태 │
├──────┼──────┼──────┼──────┼──────┼──────┤
│ 101  │ 상가 │ 50평 │ 1천만│ 50만 │ 임대중│
│ ...  │ ...  │ ...  │ ...  │ ...  │ ...  │
└──────┴──────┴──────┴──────┴──────┴──────┘
```

**수식:**

**A33:** "4. 호실별 임대 현황" (병합, 배경색, 굵게)

**A35:F35:** 헤더 (호실, 용도, 면적, 보증금, 월세, 상태)

**A36:** (QUERY 수식 - 복잡함, 아래 참조)

---

**호실 목록 QUERY 수식 (A36):**

```
=QUERY({
  상가매물!A:Z, ARRAYFORMULA(REPLICATE("상가", ROWS(상가매물!A:A)));
  원투룸!A:Z, ARRAYFORMULA(REPLICATE("원투룸", ROWS(원투룸!A:A)))
},
"SELECT Col호실, Col용도타입, Col면적, Col보증금, Col월세, Col거래상태, Col매물유형
 WHERE Col건물명 = '"&$B$5&"' AND Col건물명 IS NOT NULL
 ORDER BY Col호실", 1)
```

**주의:**
- 실제 컬럼 인덱스(Col1, Col2...)로 변경 필요
- 상가매물과 원투룸의 컬럼 구조가 동일해야 함

---

#### F. 차트 섹션 (H7:N30)

**차트 1: 임대율 도넛 차트 (H7:N16)**

1. Insert > Chart
2. Chart type: Donut chart
3. Data range: B22:B24 (총호실수, 임대중, 공실)
4. Customize:
   - Title: "임대 현황"
   - Donut hole: 0.6
   - Colors: Blue (임대중), Gray (공실)

**차트 2: 월세 분포 막대 차트 (H18:N30)**

1. Insert > Chart
2. Chart type: Column chart
3. Data range: 호실별 현황 테이블 E열 (월세)
4. Customize:
   - Title: "호실별 월세 분포"
   - Axis: 호실명

---

### Step 3: 서식 및 인쇄 설정

#### 서식

**조건부 서식:**
1. 연수익률(%) 셀 (B30):
   - 5% 미만: 빨강
   - 5-7%: 주황
   - 7-10%: 초록
   - 10% 이상: 진한 초록

**테두리:**
- 각 섹션마다 테두리 추가
- 헤더는 굵은 테두리

---

#### 인쇄 설정

**File > Print setup:**
```yaml
Orientation: Portrait
Paper size: A4
Margins: Normal
Scale: Fit to width
Page breaks: Manual (섹션 사이)
Headers & footers:
  - Header: "건물 수익률 분석표"
  - Footer: "Page [page] of [total]"
```

---

## 상가 수익률 분석표 생성

### Step 1: 새 시트 생성

**시트 이름:** `수익률분석표_상가`

---

### Step 2: 템플릿 레이아웃 구성

#### A. 헤더 섹션 (A1:G5)

```
┌─────────────────────────────────────────────────┐
│              상가 투자 수익률 분석표             │
│                                                 │
│  작성일: [자���]          담당자: [입력]         │
│                                                 │
│  상가 선택: [드롭다운] ▼                        │
└─────────────────────────────────────────────────┘
```

**셀 설정:**
- A1: "상가 투자 수익률 분석표"
- B5: 드롭다운 (List from range: `상가매물!B2:B100` - ID 컬럼)

---

#### B. 기본정보 섹션 (A7:G14)

```
┌─────────────────────────────────────────────────┐
│ 1. 상가 기본 정보                               │
├─────────────────────────────────────────────────┤
│  건물명:      [참조]                            │
│  호실:        [참조]                            │
│  주소:        [참조]                            │
│  면적:        [참조]          용도: [참조]      │
│  층:          [참조]          방향: [참조]      │
└─────────────────────────────────────────────────┘
```

**수식:**

**A7:** "1. 상가 기본 정보" (병합, 배경색, 굵게)

**A9:** "건물명:"
**B9:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("건물명", 상가매물!$1:$1, 0), FALSE), "")`

**A10:** "호실:"
**B10:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("호실", 상가매물!$1:$1, 0), FALSE), "")`

**A11:** "주소:"
**B11:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("주소", 상가매물!$1:$1, 0), FALSE), "")`

**A12:** "면적:"
**B12:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("면적(평)", 상가매물!$1:$1, 0), FALSE), "") & "평"`

**D12:** "용도:"
**E12:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("용도", 상가매물!$1:$1, 0), FALSE), "")`

**A13:** "층:"
**B13:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("층", 상가매물!$1:$1, 0), FALSE), "")`

**D13:** "방향:"
**E13:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("방향", 상가매물!$1:$1, 0), FALSE), "")`

---

#### C. 매물정보 섹션 (A16:G22)

```
┌─────────────────────────────────────────────────┐
│ 2. 매물 정보                                    │
├─────────────────────────────────────────────────┤
│  거래유형:    [참조]          거래상태: [참조]  │
│  매매가:      [참조]                   1.5억원  │
│  보증금:      [참조]                   3천만원  │
│  월세:        [참조]                   150만원  │
│  등록일:      [참조]                            │
└─────────────────────────────────────────────────┘
```

**수식:**

**A16:** "2. 매물 정보" (병합, 배경색, 굵게)

**A18:** "거래유형:"
**B18:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("거래유형", 상가매물!$1:$1, 0), FALSE), "")`

**D18:** "거래상태:"
**E18:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("거래상태", 상가매물!$1:$1, 0), FALSE), "")`

**A19:** "매매가:"
**B19:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("매매가", 상가매물!$1:$1, 0), FALSE), 0)`

**A20:** "보증금:"
**B20:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("보증금", 상가매물!$1:$1, 0), FALSE), 0)`

**A21:** "월세:"
**B21:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("월세", 상가매물!$1:$1, 0), FALSE), 0)`

**A22:** "등록일:"
**B22:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("등록일", 상가매물!$1:$1, 0), FALSE), "")`

---

#### D. 수익률 분석 섹션 (A24:G32)

```
┌─────────────────────────────────────────────────┐
│ 3. 수익률 분석                                  │
├─────────────────────────────────────────────────┤
│  연 임대수익:      1,800만원                    │
│                                                 │
│  연 수익률:        12.0%       ⭐⭐            │
│  투자회수기간:     8.3년                        │
│                                                 │
│  평당 가격:        300만원/평                   │
│  평당 월세:        3만원/평                     │
└─────────────────────────────────────────────────┘
```

**수식:**

**A24:** "3. 수익률 분석" (병합, 배경색, 굵게)

**A26:** "연 임대수익:"
**B26:** `=IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("연임대수익", 상가매물!$1:$1, 0), FALSE), 0)`

**A28:** "연 수익률:"
**B28:** `=TEXT(IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("연수익률(%)", 상가매물!$1:$1, 0), FALSE), 0)/100, "0.00%")`
(배경색: 노란색, 굵게)

**A29:** "투자회수기간:"
**B29:** `=TEXT(IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("투자회수기간(년)", 상가매물!$1:$1, 0), FALSE), 0), "0.0") & "년"`

**A31:** "평당 가격:"
**B31:** `=TEXT(IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("평당가격", 상가매물!$1:$1, 0), FALSE), 0), "#,##0") & "만원/평"`

**A32:** "평당 월세:"
**B32:** `=TEXT(IFERROR(VLOOKUP($B$5, 상가매물!$B:$Z, MATCH("평당월세", 상가매물!$1:$1, 0), FALSE), 0), "#,##0") & "만원/평"`

---

#### E. 주변 시세 비교 섹션 (A34:G42)

```
┌─────────────────────────────────────────────────┐
│ 4. 주변 시세 비교 (같은 건물 내)                │
├──────┬──────┬──────┬──────┬──────────┤
│ 호실 │ 면적 │ 매매가│ 월세 │ 수익률   │
├──────┼──────┼──────┼──────┼──────────┤
│ 101  │ 50평 │ 1.5억│ 150만│ 12.0%    │
│ ...  │ ...  │ ...  │ ...  │ ...      │
└──────┴──────┴──────┴──────┴──────────┘
```

**수식 (A36):**

```
=QUERY(상가매물!A:Z,
"SELECT Col호실, Col면적, Col매매가, Col월세, Col연수익률
 WHERE Col건물명 = '"&B9&"' AND Col건물명 IS NOT NULL
 ORDER BY Col호실", 1)
```

---

#### F. 차트 섹션 (H7:N32)

**차트 1: 수익률 게이지 차트 (H7:N18)**

1. Insert > Chart
2. Chart type: Gauge chart
3. Data range: B28 (연수익률)
4. Customize:
   - Min: 0%
   - Max: 20%
   - Red: 0-5%
   - Yellow: 5-10%
   - Green: 10-20%

**차트 2: 주변 시세 비교 차트 (H20:N32)**

1. Chart type: Bar chart (horizontal)
2. Data range: 주변 시세 비교 테이블
3. X-axis: 호실
4. Y-axis: 연수익률

---

### Step 3: 서식 및 인쇄 설정

**건물 분석표와 동일한 패턴 적용**

---

## PDF 생성 및 공유

### 방법 1: 수동 PDF 생성 (추천)

**단계:**
1. 분석표 시트 열기
2. 드롭다운에서 건물/상가 선택
3. 데이터 자동 로드 확인
4. **File > Download > PDF (.pdf)**
5. 인쇄 옵션 확인:
   - Current sheet
   - Portrait
   - Fit to width
6. Download 클릭
7. 고객에게 이메일/카카오톡 전송

**소요시간:** 30초

---

### 방법 2: 링크 공유

**단계:**
1. 분석표 시트에서 건물/상가 선택
2. **File > Share > Publish to web**
3. Link: "Entire document" → "수익률분석표_건물" 시트 선택
4. **Get link** 클릭
5. 링크 복사하여 전송

**장점:** 실시간 업데이트 반영

---

## 고급: Apps Script 자동화

### 자동 PDF 생성 스크립트

**Tools > Script editor**

#### 스크립트 1: 건물 분석표 PDF 생성

```javascript
function 건물분석표PDF생성() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var 분석표시트 = ss.getSheetByName('수익률분석표_건물');
  var 건물명 = 분석표시트.getRange('B5').getValue();

  if (!건물명) {
    SpreadsheetApp.getUi().alert('건물을 선택해주세요.');
    return;
  }

  // PDF 생성
  var url = 'https://docs.google.com/spreadsheets/d/' + ss.getId() +
            '/export?format=pdf&gid=' + 분석표시트.getSheetId() +
            '&portrait=true&fitw=true&size=A4';

  var token = ScriptApp.getOAuthToken();
  var response = UrlFetchApp.fetch(url, {
    headers: {
      'Authorization': 'Bearer ' + token
    }
  });

  // Drive 폴더에 저장
  var 폴더ID = 'YOUR_FOLDER_ID';  // Google Drive 폴더 ID 입력
  var folder = DriveApp.getFolderById(폴더ID);

  var 파일명 = 건물명 + '_수익률분석표_' +
               Utilities.formatDate(new Date(), 'Asia/Seoul', 'yyyyMMdd') +
               '.pdf';
  var blob = response.getBlob().setName(파일명);
  var file = folder.createFile(blob);

  // 완료 메시지
  var ui = SpreadsheetApp.getUi();
  var 결과 = ui.alert(
    'PDF 생성 완료',
    'PDF가 생성되었습니다.\n\n파일명: ' + 파일명 + '\n\nDrive에서 확인하시겠습니까?',
    ui.ButtonSet.YES_NO
  );

  if (결과 == ui.Button.YES) {
    var htmlOutput = HtmlService
      .createHtmlOutput('<script>window.open("' + file.getUrl() + '");google.script.host.close();</script>')
      .setWidth(250)
      .setHeight(100);
    SpreadsheetApp.getUi().showModalDialog(htmlOutput, 'PDF 열기');
  }
}
```

---

#### 스크립트 2: 상가 분석표 PDF 생성

```javascript
function 상가분석표PDF생성() {
  var ss = SpreadsheetApp.getActiveSpreadsheet();
  var 분석표시트 = ss.getSheetByName('수익률분석표_상가');
  var 상가ID = 분석표시트.getRange('B5').getValue();

  if (!상가ID) {
    SpreadsheetApp.getUi().alert('상가를 선택해주세요.');
    return;
  }

  // 상가명 가져오기 (건물명 + 호실)
  var 건물명 = 분석표시트.getRange('B9').getValue();
  var 호실 = 분석표시트.getRange('B10').getValue();
  var 상가명 = 건물명 + '_' + 호실;

  // PDF 생성 (건물 분석표와 동일한 로직)
  var url = 'https://docs.google.com/spreadsheets/d/' + ss.getId() +
            '/export?format=pdf&gid=' + 분석표시트.getSheetId() +
            '&portrait=true&fitw=true&size=A4';

  var token = ScriptApp.getOAuthToken();
  var response = UrlFetchApp.fetch(url, {
    headers: {
      'Authorization': 'Bearer ' + token
    }
  });

  var 폴더ID = 'YOUR_FOLDER_ID';
  var folder = DriveApp.getFolderById(폴더ID);

  var 파일명 = 상가명 + '_수익률분석표_' +
               Utilities.formatDate(new Date(), 'Asia/Seoul', 'yyyyMMdd') +
               '.pdf';
  var blob = response.getBlob().setName(파일명);
  var file = folder.createFile(blob);

  // 완료 메시지 (동일)
  var ui = SpreadsheetApp.getUi();
  ui.alert('PDF 생성 완료: ' + 파일명);
}
```

---

#### 스크립트 3: 메뉴 추가

```javascript
function onOpen() {
  var ui = SpreadsheetApp.getUi();
  ui.createMenu('📊 수익률 분석')
    .addItem('🏢 건물 분석표 PDF 생성', '건물분석표PDF생성')
    .addItem('🏪 상가 분석표 PDF 생성', '상가분석표PDF생성')
    .addSeparator()
    .addItem('📧 이메일 전송', '분석표이메일전송')
    .addToUi();
}
```

---

#### 스크립트 4: 이메일 전송 (보너스)

```javascript
function 분석표이메일전송() {
  var ui = SpreadsheetApp.getUi();

  // 이메일 주소 입력
  var 입력결과 = ui.prompt(
    '이메일 전송',
    '받는 사람 이메일 주소를 입력하세요:',
    ui.ButtonSet.OK_CANCEL
  );

  if (입력결과.getSelectedButton() != ui.Button.OK) {
    return;
  }

  var 이메일 = 입력결과.getResponseText();

  // 현재 시트 확인
  var 현재시트 = SpreadsheetApp.getActiveSheet().getName();
  var PDF파일;
  var 제목;

  if (현재시트 == '수익률분석표_건물') {
    // 건물 분석표 PDF 생성 로직
    건물분석표PDF생성();
    // ... (위 스크립트에서 file 객체 반환하도록 수정)
    제목 = '건물 투자 수익률 분석표';
  } else if (현재시트 == '수익률분석표_상가') {
    상가분석표PDF생성();
    제목 = '상가 투자 수익률 분석표';
  }

  // 이메일 전송
  MailApp.sendEmail({
    to: 이메일,
    subject: 제목,
    body: '안녕하세요,\n\n' + 제목 + '를 첨부합니다.\n\n감사합니다.',
    attachments: [PDF파일.getBlob()]
  });

  ui.alert('이메일 전송 완료: ' + 이메일);
}
```

---

### Apps Script 설정

**1. 권한 승인:**
- 처음 실행 시 권한 요청
- "Review Permissions" 클릭
- Google 계정 선택
- "Advanced" > "Go to [프로젝트명] (unsafe)" 클릭
- "Allow" 클릭

**2. Drive 폴더 ID 찾기:**
- Google Drive에서 PDF 저장할 폴더 열기
- 주소창 URL 확인: `https://drive.google.com/drive/folders/[FOLDER_ID]`
- FOLDER_ID 부분 복사
- 스크립트의 `YOUR_FOLDER_ID`에 붙여넣기

---

## 체크리스트

### 건물 테이블 설정
- [ ] Google Sheets에 매물 칼럼 추가 (9개)
- [ ] AppSheet에서 데이터 동기화
- [ ] 칼럼 타입 설정 (Enum, Price, Date 등)
- [ ] 가상 칼럼 추가 (9개)
- [ ] DP거래 수식 추가
- [ ] Show_If 조건 설정
- [ ] Form 뷰 테스트

### 상가 테이블 설정
- [ ] AppSheet 가상 칼럼 추가 (5개)
- [ ] 수식 테스트
- [ ] Show_If 조건 설정

### 건물 분석표
- [ ] 새 시트 생성
- [ ] 헤더 섹션 구성
- [ ] 기본정보 섹션 + 수식
- [ ] 매물정보 섹션 + 수식
- [ ] 수익률 분석 섹션 + 수식
- [ ] 호실별 현황 섹션 + QUERY
- [ ] 차트 2개 추가
- [ ] 조건부 서식 적용
- [ ] 인쇄 설정
- [ ] PDF 생성 테스트

### 상가 분석표
- [ ] 새 시트 생성
- [ ] 헤더 섹션 구성
- [ ] 기본정보 섹션 + 수식
- [ ] 매물정보 섹션 + 수식
- [ ] 수익률 분석 섹션 + 수식
- [ ] 주변 시세 비교 섹션 + QUERY
- [ ] 차트 2개 추가
- [ ] 조건부 서식 적용
- [ ] 인쇄 설정
- [ ] PDF 생성 테스트

### Apps Script (선택)
- [ ] 스크립트 작성
- [ ] 권한 승인
- [ ] Drive 폴더 ID 설정
- [ ] 메뉴 추가
- [ ] PDF 생성 테스트
- [ ] 이메일 전송 테스트

---

## 문제 해결 (Troubleshooting)

### Q1: VLOOKUP에서 #N/A 오류

**원인:** 건물명/상가ID가 정확히 일치하지 않음

**해결:**
```
=IFERROR(VLOOKUP(...), "")
```
IFERROR로 감싸서 오류 시 빈 문자열 표시

---

### Q2: QUERY 수식 오류

**원인:** 컬럼 인덱스 불일치

**해결:**
- 상가매물 시트에서 실제 컬럼 순서 확인
- Col1, Col2... 대신 실제 인덱스 번호 사용
- 예: `SELECT Col2, Col5, Col10` → `SELECT Col건물명, Col호실, Col월세`

---

### Q3: 차트가 업데이트 안 됨

**원인:** 데이터 범위가 고정됨

**해결:**
- 차트 편집 > Setup > Data range
- 범위를 동적으로 변경 (예: A36:F100)

---

### Q4: PDF 다운로드가 느림

**원인:** QUERY 수식이 많아서 계산 시간 증가

**해결:**
- 불필요한 QUERY 최소화
- 또는 Apps Script로 자동화

---

### Q5: Apps Script 권한 오류

**원인:** OAuth 권한 미승인

**해결:**
1. Extensions > Apps Script
2. 함수 실행
3. "Review Permissions" 클릭
4. 단계별 권한 승인

---

## 다음 단계

### Phase 1 완료 후
- [ ] 실제 데이터로 테스트
- [ ] 고객 피드백 수집
- [ ] 템플릿 디자인 개선

### Phase 2 (향후)
- [ ] AppSheet에서 분석표 버튼 추가
- [ ] 자동 이메일 전송
- [ ] 다중 건물 비교 분석표
- [ ] 수익률 시뮬레이터 (매매가 변화에 따른 수익률)

---

## 참고 자료

### Google Sheets 함수
- [VLOOKUP 문서](https://support.google.com/docs/answer/3093318)
- [QUERY 문서](https://support.google.com/docs/answer/3093343)
- [ARRAYFORMULA 문서](https://support.google.com/docs/answer/3093275)

### Apps Script
- [Apps Script 문서](https://developers.google.com/apps-script)
- [Spreadsheet Service](https://developers.google.com/apps-script/reference/spreadsheet)
- [Drive Service](https://developers.google.com/apps-script/reference/drive)

---

**문서 버전:** 1.0
**최종 수정:** 2025-10-30
**작성자:** Claude Code
