---
tags: [framing, 부동산, 프로젝트, 자동화, 통합]
type: project
status: 진행중
creation_date: 2025-10-15
index: [["🏷 부동산 프로젝트"]]
---

# 📋 통합 마스터플랜 - 기존 시스템 개선 + 확장

> 기존 구글 스프레드시트 매물관리 시스템을 개선하고, 데이터 수집부터 콘텐츠 발행까지 확장하는 완전 자동화 로드맵

---

## 🎯 프로젝트 목표

### 기존 시스템 현황
- ✅ Google Sheets 5개 시트 구조 (등록검색/매물DB/고객DB/아파트단지/고정값)
- ✅ Apps Script 자동화 기본 구현
- ✅ 61개 매물, 25명 고객 데이터 보유
- ⚠️ 데이터 품질 이슈 (필수 필드 70% 누락)
- ⚠️ 수동 입력 의존도 높음

### 목표 시스템
- 🎯 **기존 안정화**: 데이터 품질 95% 이상
- 🎯 **자동 수집**: PDF/CSV/API/크롤링 자동화
- 🎯 **자동 발행**: 브리핑/마케팅 콘텐츠 생성
- 🎯 **확장 가능**: 오피스텔/상가로 확장 가능한 구조

---

## 📊 Phase 0: 현황 파악 (완료)

### ✅ 기존 자산 목록

**Google Sheets 구조:**
```
1. 등록검색 (UI)
   - Row 4-28: 매물 정보 (25개 필드)
   - Row 29-36: 고객 정보 (8개 필드)
   - 매물ID/고객ID 자동 생성 로직

2. 매물DB (핵심 데이터)
   - 61개 매물 데이터
   - 주요 단지: 힐스테이트두정역(37건), 천안아이파크(12건)

3. 고객DB (CRM)
   - 25명 데이터 (매도인 80%, 손님 20%)
   - 상담이력 필드 미사용

4. 아파트단지 (마스터)
   - 8개 단지 정보
   - 40개 속성 (건축/분양/주차 등)

5. 고정값 (설정)
   - 드롭다운 소스
```

**Apps Script 코드:**
```
- 통합_최종_스크립트.js (가장 최신)
- 매물등록수정_통합스크립트.js
- 옵션불러오기_앱스스크립트_수정본.js
```

**n8n 워크플로우:**
```
- n8n-workflow-read-sheets.json
- n8n-workflow-read-normalized-sheets.json
```

**CSV 데이터:**
```
- 통합단지DB - 분양가.csv (148개 타입)
- 통합단지DB - 옵션.csv (옵션 정보)
- 통합단지DB - 타입.csv
- 통합단지DB - 발코니.csv
- 통합단지DB - 단지일정.csv
```

### ⚠️ 발견된 문제점

**데이터 품질:**
- ❌ 거래유형 누락 32.8%
- ❌ 거래상태 누락 73.8%
- ❌ 매물ID 미생성 90.2%
- ❌ 접수일 누락 65.6%

**사용성:**
- ❌ 고객DB 활용도 낮음 (상담이력 0%)
- ❌ Google Drive 연동 거의 없음 (1.6%)
- ❌ 중복 매물 방지 불완전 (타입 미반영)

---

## 🚀 Phase 1: 기존 시스템 안정화 (1주)

### 목표: 데이터 품질 95% 달성

#### 1.1 Apps Script 개선 (2일)

**필수 필드 검증 강화:**
```javascript
function registerPropertyAndClient() {
  // 기존 코드에 추가
  const requiredFields = ['단지명', '동', '호', '타입', '거래유형', '거래상태'];

  for (let field of requiredFields) {
    if (!dataMap[field]) {
      Browser.msgBox('필수 항목 누락', `${field}을(를) 입력해주세요.`, Browser.Buttons.OK);
      return;
    }
  }

  // 접수일 자동 입력
  if (!dataMap['접수일']) {
    dataMap['접수일'] = new Date();
  }

  // 접수자 자동 입력 (현재 사용자)
  if (!dataMap['접수자']) {
    dataMap['접수자'] = Session.getActiveUser().getEmail().split('@')[0];
  }
}
```

**매물ID 자동 생성 수정:**
```javascript
function generatePropertyId(apartmentName, dong, ho, type) {
  const apartmentSheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('아파트단지');
  const data = apartmentSheet.getDataRange().getValues();

  // 단지명축약 찾기
  let shortName = '';
  for (let i = 1; i < data.length; i++) {
    if (data[i][0] === apartmentName) {
      shortName = data[i][4]; // E열: 단지명축약
      break;
    }
  }

  return `${shortName} ${dong}-${ho}-${type}`;
}
```

**중복 매물 방지 (타입 포함):**
```javascript
// 등록검색 C3 셀 수식 수정
=IF(C4=""&C5=""&C6=""&C7="", "",
   IF(COUNTA(FILTER(매물DB!A:A,
      매물DB!A:A=C4,
      매물DB!B:B=C5,
      매물DB!C:C=C6,
      매물DB!D:D=C7))>0,  // 타입 추가!
   "🏠 매물 있음 (수정 모드)",
   "✨ 신규 매물 (등록 모드)"))
```

#### 1.2 데이터 정제 (1일)

**일괄 처리 스크립트:**
```javascript
function cleanupExistingData() {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const propertyDb = ss.getSheetByName('매물DB');
  const data = propertyDb.getDataRange().getValues();

  for (let i = 1; i < data.length; i++) { // 헤더 제외
    let row = data[i];
    let updated = false;

    // 매물ID 생성
    if (!row[27]) { // 매물ID 열 (28번째 열)
      row[27] = generatePropertyId(row[0], row[1], row[2], row[3]);
      updated = true;
    }

    // 거래상태 기본값
    if (!row[8]) { // 거래상태 열
      row[8] = '접수';
      updated = true;
    }

    // 접수일 기본값 (오늘)
    if (!row[26]) {
      row[26] = new Date();
      updated = true;
    }

    if (updated) {
      propertyDb.getRange(i + 1, 1, 1, row.length).setValues([row]);
    }
  }

  Browser.msgBox('완료', `${data.length - 1}개 매물 데이터 정제 완료`, Browser.Buttons.OK);
}
```

#### 1.3 사용성 개선 (2일)

**고객 상담이력 자동 기록:**
```javascript
function addCustomerNote(customerId, note) {
  const customerDb = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('고객DB');
  const data = customerDb.getDataRange().getValues();

  for (let i = 1; i < data.length; i++) {
    if (data[i][0] === customerId) { // 고객ID 매칭
      const today = Utilities.formatDate(new Date(), 'GMT+9', 'yyyy-MM-dd');
      const existingNotes = data[i][7] || ''; // 메모 열
      const newNotes = `[${today}] ${note}\n${existingNotes}`;

      customerDb.getRange(i + 1, 8).setValue(newNotes);
      break;
    }
  }
}
```

**Google Drive 자동 폴더 생성:**
```javascript
function createPropertyFolder() {
  const propertyId = regSheet.getRange('C28').getValue(); // 매물ID
  const parentFolderId = 'YOUR_PARENT_FOLDER_ID'; // 매물 관리 메인 폴더

  const parentFolder = DriveApp.getFolderById(parentFolderId);
  const newFolder = parentFolder.createFolder(propertyId);

  // 폴더 URL을 시트에 자동 입력
  regSheet.getRange('C24').setValue(newFolder.getUrl());

  return newFolder.getUrl();
}
```

#### 1.4 테스트 및 검증 (2일)

- [ ] 신규 매물 등록 10건 테스트
- [ ] 기존 매물 수정 5건 테스트
- [ ] 필수 필드 검증 테스트
- [ ] 매물ID/고객ID 자동 생성 검증
- [ ] 중복 매물 방지 테스트

**예상 결과:**
- 데이터 품질 95% 이상
- 매물ID 생성률 100%
- 필수 필드 입력률 100%

---

## 📥 Phase 2: 데이터 수집 자동화 (2주)

### 목표: 수동 입력 50% 감소

#### 2.1 CSV 자동 가져오기 (3일)

**Python 스크립트 (새 프로젝트):**
```python
# csv_to_sheets.py
import gspread
import pandas as pd
from oauth2client.service_account import ServiceAccountCredentials

def upload_csv_to_sheets(csv_path, sheet_name, worksheet_name):
    # 인증
    scope = ['https://spreadsheets.google.com/feeds']
    creds = ServiceAccountCredentials.from_json_keyfile_name(
        'credentials.json', scope
    )
    client = gspread.authorize(creds)

    # CSV 읽기
    df = pd.read_csv(csv_path, encoding='cp949')

    # Sheets 업데이트
    sheet = client.open(sheet_name).worksheet(worksheet_name)
    sheet.update([df.columns.values.tolist()] + df.values.tolist())

    print(f"✅ {csv_path} → {worksheet_name} 업로드 완료")

# 실행
upload_csv_to_sheets('통합단지DB - 분양가.csv', '아파트_매물_DB', '분양가')
```

**Apps Script로 CSV 파싱:**
```javascript
function importCSVFromDrive(fileId) {
  const file = DriveApp.getFileById(fileId);
  const csvData = Utilities.parseCsv(file.getBlob().getDataAsString('CP949'));

  const targetSheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('분양가');
  targetSheet.getRange(1, 1, csvData.length, csvData[0].length).setValues(csvData);
}
```

#### 2.2 PDF 입주자공고문 파싱 (5일)

**Python + pdfplumber:**
```python
# pdf_parser.py
import pdfplumber
import re

def parse_apartment_notice(pdf_path):
    """입주자모집공고문에서 핵심 정보 추출"""
    with pdfplumber.open(pdf_path) as pdf:
        text = ''
        for page in pdf.pages:
            text += page.extract_text()

    # 정규식으로 데이터 추출
    data = {
        '단지명': extract_apartment_name(text),
        '총세대수': extract_total_units(text),
        '입주예정': extract_move_in_date(text),
        '분양가': extract_prices(text)
    }

    return data

def extract_apartment_name(text):
    # "○○아파트 입주자모집공고" 패턴
    match = re.search(r'(.+?)\s*입주자모집공고', text)
    return match.group(1) if match else None
```

**Sheets 연동:**
```python
def upload_parsed_data_to_sheets(pdf_data):
    # ... (gspread 코드)

    # 아파트단지 시트에 추가
    apartment_sheet.append_row([
        pdf_data['단지명'],
        pdf_data['단지코드'],
        pdf_data['총세대수'],
        # ...
    ])
```

#### 2.3 웹 크롤링 (네이버 부동산) (4일)

**Playwright 활용:**
```python
# naver_crawler.py
from playwright.sync_api import sync_playwright

def crawl_naver_realestate(apartment_name):
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True)
        page = browser.new_page()

        # 네이버 부동산 검색
        page.goto(f'https://new.land.naver.com/search?sk={apartment_name}')
        page.wait_for_selector('.item_list')

        # 매물 정보 추출
        listings = page.query_selector_all('.item_inner')
        data = []

        for listing in listings:
            price = listing.query_selector('.price').inner_text()
            area = listing.query_selector('.spec').inner_text()
            floor = listing.query_selector('.floor').inner_text()

            data.append({
                '가격': parse_price(price),
                '면적': parse_area(area),
                '층수': floor
            })

        browser.close()
        return data
```

#### 2.4 공공 API 연동 (2일)

**국토교통부 실거래가 API:**
```python
# api_client.py
import requests

def get_real_price(region_code, date):
    """실거래가 정보 조회"""
    url = 'http://openapi.molit.go.kr/OpenAPI_ToolInstallPackage/service/rest/RTMSOBJSvc/getRTMSDataSvcAptTradeDev'

    params = {
        'serviceKey': 'YOUR_API_KEY',
        'LAWD_CD': region_code,
        'DEAL_YMD': date,
        'numOfRows': '100'
    }

    response = requests.get(url, params=params)
    # XML 파싱 후 DataFrame 변환
    # ...
```

---

## 📤 Phase 3: 콘텐츠 자동 발행 (2주)

### 목표: 일일 브리핑 자동화

#### 3.1 일일 브리핑 자동 생성 (3일)

**Apps Script 트리거:**
```javascript
function dailyBriefing() {
  const today = new Date();
  const yesterday = new Date(today.getTime() - 24*60*60*1000);

  // 어제 등록된 매물 조회
  const propertyDb = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('매물DB');
  const data = propertyDb.getDataRange().getValues();

  const newListings = data.filter(row => {
    const registerDate = new Date(row[26]); // 접수일
    return registerDate >= yesterday && registerDate < today;
  });

  // Google Docs 생성
  const doc = DocumentApp.create(`일일 브리핑 ${Utilities.formatDate(today, 'GMT+9', 'yyyy-MM-dd')}`);
  const body = doc.getBody();

  body.appendParagraph('🏢 신규 매물 브리핑').setHeading(DocumentApp.ParagraphHeading.HEADING1);
  body.appendParagraph(`📅 ${Utilities.formatDate(today, 'GMT+9', 'yyyy년 MM월 dd일')}`);
  body.appendParagraph(`📊 신규 매물: ${newListings.length}건`);

  // 매물 목록
  newListings.forEach((listing, index) => {
    body.appendParagraph(`${index + 1}. ${listing[0]} ${listing[1]}동 ${listing[2]}호 ${listing[3]}타입`);
    body.appendParagraph(`   💰 ${listing[7]} / 거래유형: ${listing[5]}`);
  });

  // 이메일 발송
  MailApp.sendEmail({
    to: 'team@example.com',
    subject: `[일일 브리핑] ${newListings.length}건 신규 매물`,
    body: `브리핑 문서: ${doc.getUrl()}`
  });
}

// 매일 오전 9시 자동 실행 트리거 설정
function createDailyTrigger() {
  ScriptApp.newTrigger('dailyBriefing')
    .timeBased()
    .everyDays(1)
    .atHour(9)
    .create();
}
```

#### 3.2 마케팅 문구 자동 생성 (5일)

**Gemini API 연동:**
```python
# marketing_content.py
import google.generativeai as genai

def generate_listing_description(property_data):
    """매물 정보를 기반으로 마케팅 문구 생성"""
    genai.configure(api_key='YOUR_API_KEY')
    model = genai.GenerativeModel('gemini-pro')

    prompt = f"""
    다음 아파트 매물에 대한 매력적인 설명 문구를 작성해주세요.

    - 아파트: {property_data['단지명']}
    - 면적: {property_data['타입']}
    - 가격: {property_data['매매가']}만원
    - 특징: {property_data['방향']}, {property_data['층수']}

    형식:
    1. 제목 (20자 이내, 임팩트 있게)
    2. 본문 (150자 이내, 핵심 장점 3가지)
    3. 해시태그 5개
    """

    response = model.generate_content(prompt)
    return response.text
```

**Sheets 연동:**
```python
def add_marketing_content_to_sheets(property_id, content):
    # ... (gspread 코드)
    marketing_sheet.append_row([
        property_id,
        content['제목'],
        content['본문'],
        content['해시태그'],
        datetime.now()
    ])
```

#### 3.3 인스타그램 콘텐츠 생성 (4일)

**Canva API (또는 PIL):**
```python
# instagram_post.py
from PIL import Image, ImageDraw, ImageFont

def create_listing_image(property_data):
    """매물 정보를 시각화한 이미지 생성"""
    img = Image.new('RGB', (1080, 1080), color='white')
    draw = ImageDraw.Draw(img)

    # 폰트
    title_font = ImageFont.truetype('NanumGothicBold.ttf', 60)
    text_font = ImageFont.truetype('NanumGothic.ttf', 40)

    # 제목
    draw.text((50, 50), property_data['단지명'], fill='black', font=title_font)

    # 핵심 정보
    draw.text((50, 200), f"📍 {property_data['타입']}", fill='navy', font=text_font)
    draw.text((50, 300), f"💰 {property_data['매매가']}만원", fill='green', font=text_font)

    # 저장
    img.save(f"instagram_{property_data['매물ID']}.png")
```

#### 3.4 블로그 자동 포스팅 (2일)

**WordPress API:**
```python
# blog_post.py
import requests

def post_to_blog(title, content, tags):
    """WordPress 블로그에 자동 포스팅"""
    url = 'https://yourblog.com/wp-json/wp/v2/posts'

    headers = {
        'Authorization': 'Bearer YOUR_TOKEN',
        'Content-Type': 'application/json'
    }

    data = {
        'title': title,
        'content': content,
        'status': 'publish',
        'tags': tags
    }

    response = requests.post(url, json=data, headers=headers)
    return response.json()
```

---

## 🎯 Phase 4: 통합 및 최적화 (1주)

### 4.1 전체 워크플로우 통합

**자동화 흐름:**
```
[매일 새벽 3시]
├── Python: 공공 API 실거래가 수집 → Sheets 업데이트
├── Python: 네이버 부동산 크롤링 → Sheets 업데이트
└── Apps Script: 데이터 중복 제거 & 정규화

[매일 오전 9시]
├── Apps Script: 일일 브리핑 생성 → Google Docs
├── Apps Script: 브리핑 이메일 발송
└── Python: 마케팅 문구 생성 → Sheets 저장

[신규 매물 등록 시]
├── Apps Script: 매물ID 자동 생성
├── Apps Script: Google Drive 폴더 생성
├── n8n: Slack 알림 발송
└── Python: 인스타그램 이미지 생성
```

### 4.2 대시보드 추가

**새 시트 생성: "대시보드"**
```
피벗 테이블:
- 단지별 매물 현황
- 거래유형별 통계
- 월별 접수 추이
- 담당자별 성과

차트:
- 가격 분포 히스토그램
- 시계열 추세 그래프
```

---

## 📁 프로젝트 구조 (최종)

### Git 저장소
```
apartment-automation/
├── src/
│   ├── collectors/          # 데이터 수집
│   │   ├── csv_importer.py
│   │   ├── pdf_parser.py
│   │   ├── naver_crawler.py
│   │   └── api_client.py
│   ├── processors/          # 데이터 처리
│   │   ├── normalizer.py
│   │   └── validator.py
│   ├── generators/          # 콘텐츠 생성
│   │   ├── briefing.py
│   │   ├── marketing.py
│   │   └── instagram.py
│   └── sheets/              # Sheets 연동
│       ├── writer.py
│       └── reader.py
├── apps-script/             # Google Apps Script
│   ├── 01_register.gs      # 매물 등록
│   ├── 02_validation.gs    # 검증
│   ├── 03_briefing.gs      # 브리핑
│   └── 04_triggers.gs      # 트리거
├── config/
│   ├── credentials.json
│   └── settings.yaml
├── data/
│   ├── raw/                 # 원본 CSV
│   └── processed/
├── tests/
├── requirements.txt
└── README.md
```

### Obsidian 노트
```
Flow System/
└── - Flow/01. Framing/Project/
    └── 구글 스프레드시트 자동화 매물관리 시스템구축/
        ├── 📋 통합 마스터플랜 (이 문서)
        ├── 구글 스프레드시트 자동화 매물관리 시스템 구축.md
        ├── 시트 구조 분석 보고서.md
        ├── 정규화 데이터 연동 방안.md
        └── code/ → (심볼릭 링크)
            └── D:/Projects/apartment-automation/
```

---

## ✅ 성공 지표 (KPI)

### Phase 1 완료 기준
- [ ] 데이터 품질 95% 이상
- [ ] 매물ID 자동 생성률 100%
- [ ] 필수 필드 입력률 100%

### Phase 2 완료 기준
- [ ] CSV 자동 가져오기 작동
- [ ] PDF 파싱 정확도 90% 이상
- [ ] 실거래가 자동 수집 (주 1회)

### Phase 3 완료 기준
- [ ] 일일 브리핑 100% 자동화
- [ ] 마케팅 문구 자동 생성 (매물당 10초)
- [ ] 인스타그램 이미지 자동 생성

### 최종 목표
- ⏰ 데이터 입력 시간 90% 단축
- 📊 데이터 정확도 99% 이상
- 🚀 콘텐츠 발행 100% 자동화
- 💰 월 매출 30% 증가 (효율성 증대)

---

## 🔗 관련 문서

- [[🏢 아파트 매물관리 자동화 시스템 마스터플랜]] (새 제안)
- [[30. OKR/03. Project/구글 스프레드시트 자동화 매물관리 시스템구축/구글 스프레드시트 자동화 매물관리 시스템 구축]] (기존)
- [[시트 구조 분석 보고서]] (기존)
- [[아파트 매물관리 시스템 작업 지침서]] (기존)
- [[프로젝트 허브]]

---

## 🎯 다음 단계 (지금 바로!)

### 오늘 할 일 (Phase 1 시작)

1. **Apps Script 개선** (2시간)
   - [ ] 필수 필드 검증 추가
   - [ ] 매물ID 자동 생성 수정
   - [ ] 중복 매물 방지 로직 개선

2. **데이터 정제 스크립트 실행** (1시간)
   - [ ] `cleanupExistingData()` 함수 추가
   - [ ] 61개 매물 데이터 일괄 정제
   - [ ] 결과 확인

3. **Git 저장소 생성** (30분)
   - [ ] `D:/Projects/apartment-automation` 폴더 생성
   - [ ] Git 초기화
   - [ ] GitHub 저장소 생성
   - [ ] 심볼릭 링크 연결

---

**🚀 "완벽한 새 시스템보다, 기존 시스템을 개선하는 게 먼저다!"**
