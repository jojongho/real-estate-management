---
type: output
flow_step: weaving
flow_topic: 600. 부동산
tags: [부동산, 자동화, API, GoogleAppsScript, n8n]
created: 2026-02-02
---

# 🏗️ 건축물대장 자동화 시스템 구축 완료 보고서

## 1. 개요
Google Sheets와 Notion(n8n)에서 주소를 입력하면 자동으로 **건축물대장 표제부 정보(대지면적, 연면적, 용적률 등)**를 가져오는 자동화 시스템 구축을 완료했습니다.

## 2. 주요 파일 및 노트 정리

### 📄 Google Apps Script (`BuildingLedger.gs`)
- **위치**: `/project/real-estate-management/apps-script/BuildingLedger.gs`
- **역할**: 구글 스프레드시트 내 '주소' 컬럼을 읽어 건축물 정보를 채워줌.
- **기술적 해결 사항**:
    1.  **API 변경**: 기존 `BldRgstService_v2` (구형) 대신 **`BldRgstHubService` (건축HUB)** API 도입.
    2.  **파라미터 표준 준수**: 최신 API 스펙에 맞춰 `sigungu_code`, `plat_code` (snake_case) 사용.
    3.  **인증키 처리**: Apps Script 특성상 **인코딩된(Encoded) 인증키**(`Z%2BM...`)를 직접 사용하여 URL 구성.
    4.  **에러 처리**: JSON 파싱 에러 및 데이터 부재 시 명확한 에러 메시지 출력.

### ⚡ n8n Workflow (`건축물대장 조회 (Notion 버튼).json`)
- **위치**: `/System/07. Workflows/n8n/건축물대장 조회 (Notion 버튼).json`
- **역할**: Notion에서 버튼 클릭 시 웹훅을 통해 해당 페이지의 주소를 읽어 정보를 업데이트.
- **특이사항**:
    - Vworld API로 PNU 추출 후 HUB API 호출 로직 동일 적용.
    - n8n은 **디코딩된 키**를 사용해도 내부적으로 처리가 가능함.

## 3. 핵심 기술 노트 (나중을 위한 기록 📝)

| 구분 | 내용 | 비고 |
| :--- | :--- | :--- |
| **API Endpoint** | `https://apis.data.go.kr/1613000/BldRgstHubService/getBrTitleInfo` | **건축HUB** 서비스 사용 필수 |
| **필수 파라미터** | `sigungu_code`, `bdong_code`, `plat_code`, `bun`, `ji` | **snake_case** 주의 (camelCase ❌) |
| **플랫폼 구분** | `plat_code`: 0(대지), 1(산), 2(블록) | Vworld의 `sanFlag` (1/2)와 매핑 필요 |
| **인증키 (서비스키)** | **Apps Script**: 인코딩 키 (`%` 포함)<br>**n8n/Postman**: 디코딩 키 (`+` 포함) | 환경에 따라 인코딩 여부가 다름 |
| **주소 변환** | **Vworld API** 필수 | 주소 -> PNU(19자리 코드) 변환 없이는 조회 불가 |

## 4. 향후 활용 가이드
1.  **스프레드시트**: 메뉴 > `건축물대장` > `조회 실행` 클릭.
2.  **Notion**: n8n 워크플로우를 활성화(Active)하고 Notion 버튼 프로퍼티와 연결.

---
*작성일: 2026-02-02*
*작성자: GEMINI (AI Agent)*
